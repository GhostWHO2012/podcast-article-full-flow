# 项目介绍

## 1. 项目定位

`youtube-to-weixin-article` 是一个把访谈内容转成深度长文和公众号长图的智能体技能项目。

它解决的问题是：当编辑拿到 YouTube 视频、播客字幕、截图、原始文章和参考公众号长图等资料时，如何让智能体稳定生成一篇接近晚点深度长文形态的中文图文稿，并输出可检查的竖向长图。

这个仓库不是单纯存放实验结果，也不是只保存一个提示词，而是把“样本观察、独立生成、独立评分、反馈整改、技能发布”整理成一个可复用项目。

## 2. 目标用户

这个项目适合以下用户：

- 需要把英文视频、播客或访谈改写成中文公众号文章的内容编辑；
- 需要从长字幕中提炼标题、金句、小节和截图位置的内容团队；
- 需要复用一套固定标准生成公众号长图的运营人员；
- 需要评估和迭代智能体技能的项目负责人。

## 3. 核心能力

| 能力 | 说明 |
| --- | --- |
| 标题改写 | 不直译英文标题，而是提炼中文读者更关心的身份、数字、反差和判断。 |
| 字幕重组 | 把时间线字幕改写成主题化公众号文章。 |
| 金句选择 | 从材料中提炼三条能支撑全文的开头金句。 |
| 截图判断 | 判断哪些视频帧适合用于证明、解释或增强现场感。 |
| 长图生成 | 输出 1200px 宽、单栏、可读性稳定的公众号竖向长图。 |
| 来源控制 | 页尾展示公开来源链接，不暴露本地文件名和路径。 |

## 3.1 项目分层

本项目分为两层：

| 层级 | 说明 |
| --- | --- |
| 训练和学习部分 | 用晚点原文、晚点原文长图、同源视频、字幕和截图建立对照关系，记录在 `EXPERIMENT.md`、`EVALUATION.md`、`SKILL_REVIEW.md`、`references/` 和 `evals/` 中。 |
| 用户使用部分 | `youtube-to-weixin-article/` 是最终可安装 Skill，只向用户提供“访谈内容转深度长文和公众号长图”这一项核心能力。 |

## 4. 项目流程

```text
原始公众号长图
        ↓
提炼目标风格和质量基准
        ↓
同源视频、字幕、截图资料
        ↓
独立对话框调用技能生成测试结果
        ↓
独立评分窗口评价测试结果
        ↓
汇总问题和优化建议
        ↓
用户确认后写回技能
        ↓
校验并上传 GitHub
```

这个流程强调隔离：学习、生成、评分和整改不是混在同一个上下文里完成，而是尽量分开，减少互相影响。

## 5. 仓库内容

| 文件 | 作用 |
| --- | --- |
| `README.md` | GitHub 首页，给新读者快速理解项目。 |
| `PROJECT.md` | 项目定位、目标用户、能力边界和使用方式。 |
| `EXPERIMENT.md` | 实验设计、样本来源、对比方法和主要发现。 |
| `EVALUATION.md` | 独立评分流程、评分标准和评分记录。 |
| `SKILL_REVIEW.md` | 技能评审、问题定位、整改和发布记录。 |
| `references/` | 已确认可复用的标题、结构、截图、长图和项目包装规则。 |
| `evals/` | 每一轮独立评分、对照实验或评估摘要。 |
| `youtube-to-weixin-article/SKILL.md` | 可安装的技能主体。 |
| `youtube-to-weixin-article/scripts/render_weixin_long_image.py` | 长图渲染辅助脚本。 |

## 6. 如何使用

安装技能：

```bash
npx skills add https://github.com/GhostWHO2012/podcast-article-full-flow --skill youtube-to-weixin-article
```

推荐请求：

```text
使用 youtube-to-weixin-article。
请只读取这个视频资料文件夹，生成公众号文章、截图建议和公众号竖向长图。
要求事实只来自我提供的材料，长图必须包含可用截图，页尾只显示公开来源链接。
```

## 7. 如何继续迭代

后续每一次改进都按同一套流程处理：

1. 先记录新的测试样本和生成结果。
2. 用独立窗口评分。
3. 汇总问题，不直接写入技能。
4. 用户确认哪些建议值得成为通用规则。
5. 修改 `SKILL.md`。
6. 运行校验。
7. 更新文档并上传 GitHub。

这样仓库不仅能展示结果，也能说明结果是如何被验证和改进的。

## 8. 后续项目包装标准

之后维护同类 Skill 项目时，按 [`references/project-packaging-standard.md`](references/project-packaging-standard.md) 执行：

- README 先讲清项目价值、交付物、安装方式、证据链和边界；
- `references/` 只放已经确认的通用规则；
- `evals/` 记录独立评分和测试结果；
- 没有做过的自动化测试、独立评估或效果对照不写成既成事实；
- 原始公众号长图、视频、字幕和本地样本不默认上传公开仓库。
