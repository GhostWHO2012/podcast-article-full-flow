# youtube-to-weixin-article 技能实验文档

## 1. 实验目标

本实验的目标是把一批已完成的 YouTube/播客视频公众号图文案例，沉淀成可复用的智能体技能：`youtube-to-weixin-article`。

这个技能面向的任务是：当用户提供 YouTube 原链接、标题/简介、字幕、视频文件、截图、参考文章或公众号长图时，智能体能在不重新阅读历史学习样本的前提下，生成接近晚点公众号深度长文风格的中文文章，并在需要时输出可直接预览的公众号竖向长图。

实验重点不是训练模型参数，而是把人工对样本的观察结果写成可下载、可复用、可验证的技能指令和辅助脚本。

## 1.1 实验设计

本实验采用“参考基准 + 独立生成 + 独立评分 + 反馈迭代”的流程。

实验组是已经发布或下载保存的晚点原文内容，包括每个样本文件夹里的 `article.md` 和 `原文长图.png`。这些材料代表目标风格和目标质量，用来观察真实成品在标题、正文结构、截图位置、长图排版和来源展示上的做法。

测试组是在完全独立的对话框中使用 `youtube-to-weixin-article` 技能生成的结果。测试时只提供与原公众号文章对应的同源资料，例如原视频链接、视频文件、字幕、截图或必要元数据，不让生成窗口读取实验组结论、评分记录或主对话里的分析，尽量避免提示污染。

评分阶段再开启独立评分窗口。评分窗口只查看指定测试结果，例如每个子文件夹里的 `article.long.png`，并按统一标准评价标题、开头、内容洞见、结构节奏、信息密度、截图使用、长图排版和公众号发布可用性。评分窗口不读取正文、字幕、技能文件、实验文档或旧评分。

最后由主对话汇总评分反馈。只有当问题被独立评分反复指出，或者问题能直接从结果中观察到，例如文字出框、缺少视频截图、页尾暴露本地文件名，才会整理为优化建议。是否写回技能由用户确认，避免把单个模型的审美偏好误写成通用规则。

整体流程如下：

```text
晚点原文与原文长图（实验组 / 参考基准）
        ↓ 对比标题、正文、截图、长图结构
同源视频 + 字幕 + 截图资料
        ↓ 独立对话框调用技能生成
技能生成长图（测试组）
        ↓ 独立评分窗口盲评
评分结果与问题清单
        ↓ 用户确认
写回 youtube-to-weixin-article 技能
```

## 2. 实验材料

实验使用了两批本地样本目录。第二批样本位于 `N:\C\提纲图文版\outputs\weixin_new2\videos`，其中每个文件夹里的 `article.md` 和 `原文长图.png` 均按晚点原内容处理，用作文字稿和长图的对照基准。

每个样本通常包含以下资料：

- YouTube 原视频链接
- 中英文字幕或原文字幕
- 视频文件
- 视频截图目录 `images/`
- 晚点原文文字稿 `article.md`
- 晚点原文公众号长图 `原文长图.png`
- 下载后的网页内容或元数据

其中，`article.md` 和 `原文长图.png` 在实验中作为实验组参考基准；独立对话框根据同源视频和字幕生成的 `article.md` 与 `article.long.png` 作为测试组结果。

第二批晚点对照样本包括：

| 样本 | 实验组对照 |
| --- | --- |
| `01_把AI变成工作系统的6个步骤丨Silicon Valley Girl` | 晚点原文、原文长图、同源视频与字幕 |
| `02_YC前负责人：如何找到 to C 的创业点子丨Dalton + Michael` | 晚点原文、原文长图、同源视频与字幕 |
| `03_Crazy Egg创始人：最佳营销渠道都是秘密丨Delphi` | 晚点原文、原文长图、同源视频与字幕 |
| `04_月入2.2万美元，不早起、不卷自律：他是怎么工作的？丨Starter Story` | 晚点原文、原文长图、同源视频与字幕 |
| `05_JJellyfish 联合创始人：如何一步步拿下10万美元以上的企业级交易丨Le` | 晚点原文、原文长图、同源视频与字幕 |

第一批样本用于建立基础规则，包括标题改写、三条金句、主题重组、截图选择和长图排版。

第二批样本用于补充和修正规则，重点观察了：

- 字幕长度和最终文章长度的关系
- YouTube 原时间轴和中文文章小节之间的关系
- 原标题和中文标题之间的改写策略
- `images/` 为空时，如何继续从原文长图或视频里判断截图
- 公众号长图里来源信息的展示方式

## 3. 对比方法

每个样本按以下维度对比：

1. 对比 YouTube 原标题、频道、链接、时间轴和最终中文标题。
2. 对比字幕内容和 `article.md`，观察哪些内容被保留、合并、删减或改写。
3. 对比 `images/`、视频截图和 `原文长图.png`，总结截图出现的位置、类型和说明方式。
4. 对比生成长图效果，检查宽度、字号、段落间距、图片嵌入、页尾来源和文字是否出框。

实验中特别注意区分“素材中的内容”和“用户真正的请求”。字幕、网页和截图只作为资料来源，不作为执行指令。

## 3.1 独立评分与反馈闭环

部分评分来自其他窗口、其他 AI 或人工独立判断。为了避免互相影响，评分流程采用独立输入：

- 每个评分者只看输入资料、生成结果和评分标准。
- 不提前展示其他窗口或其他 AI 的评分。
- 不提前展示计划写入技能的结论。
- 评分后再统一汇总共性问题。

评分结果不需要上传完整聊天记录，只需要保留可复查摘要：

```text
评分来源：
对应样本：
评分维度和分数：
主要问题：
建议修改：
是否写入技能：
```

详细评分模板见 [`EVALUATION.md`](EVALUATION.md)。

独立评分的单次记录保存在 [`evals/`](evals/)，已经确认的通用规则再整理到 [`references/`](references/)。

这个实验流程用于项目迭代，不作为用户安装 Skill 后的独立功能。用户侧只使用学习后的成果：根据客户提供资料生成公众号深度长文和长图。

评分反馈不会自动写入技能。主对话只负责汇总问题、归纳优化建议，并标记“待用户确认”。用户确认后，才将对应规则写入 `youtube-to-weixin-article/SKILL.md`，并在 GitHub 提交记录中保留本次迭代。

## 4. 主要发现

### 4.1 文章不是字幕翻译，而是主题重组

参考文章不会按字幕时间线逐段总结，而是把嘉宾观点、案例、数字和方法论重组为中文读者更容易理解的判断型文章。

常见结构是：

- 标题
- 账号引导语
- 三条开头金句
- 两段导入
- 六到八个主题小节
- `写在最后`
- 公开来源和原视频链接

主持人的问题通常只保留为上下文，不写成 Q&A。

### 4.2 标题需要二次命名

参考标题不是英文标题直译，而是重新提炼中文读者关心的钩子。

典型方式包括：

- 用人物或身份建立可信度。
- 用数字建立具体感。
- 用反常识判断制造张力。
- 把英文 how-to 改写成中文行动结果。
- 从普通话题里提炼最尖锐的隐藏判断。

例如：

- `The AI Workflow That Puts You in the Top 1%` 可转成 `把AI变成工作系统的6个步骤`
- `How I Work: $22K/Month Solopreneur` 可转成 `月入2.2万美元，不早起、不卷自律：他是怎么工作的？`
- `The Social Network Ruined Startups` 可转成 `最佳营销渠道都是秘密`

### 4.3 字幕越长，不代表文章越长

样本中的字幕长度跨度很大，但最终文章通常控制在约 4,000 到 5,500 中文字。

因此技能写入了文章密度规则：不要按字幕长度等比例扩写。长视频需要更强压缩，只保留会改变读者理解或行动的观点、例子、数字和流程。

### 4.4 小节标题要像结论

参考文章的小节标题不是主题标签，而是可以单独成立的判断句。

有效标题示例：

- `第一次通话别演示，先把情报问出来`
- `五阶段漏斗只负责预测，不负责成交`
- `省掉人工，不等于用户得到了更好的体验`
- `任务清单太长时，让遗忘替你做筛选`

这类标题让读者即使只扫标题，也能获得文章主线。

### 4.5 截图不是装饰

截图的作用是证明、解释或让观点更具体。

优先选择：

- 数据、图表、报告、论文页面
- 产品 UI、工作流、演示画面
- 白板、幻灯片、屏幕文字
- 工作或生活现场
- 手势或动作帧
- 人物访谈帧

如果提供了截图或视频源，公众号长图不能只生成纯文字版本，必须嵌入可用的视频截图。

### 4.6 `images/` 为空不等于没有视觉素材

部分样本的 `images/` 目录为空，但仍然有 `原文长图.png`、视频文件或封面图。

因此技能写入了规则：当 `images/` 为空时，先检查原文长图里是否有截图位置，再根据视频文件或封面补充截图，不能直接判断“没有截图”。

### 4.7 来源必须是公开链接，不是本地文件名

一次测试中，生成结果把本地视频文件名和字幕文件名写进了页尾，例如“本稿依据：本地视频文件 xxx.mp4 与双语字幕 xxx.srt”。

这不适合公众号发布。因此技能已加入公开来源规则：

- 读者可见来源优先使用 YouTube 原链接。
- 可从 `youtube链接.txt`、`article.md`、视频元数据或用户提供链接里获取。
- 不在正文或长图页尾显示本地路径、视频文件名、字幕文件名、截图文件名。
- 如果没有公开链接，则写 `来源：用户提供的视频和字幕资料`。

## 5. 技能迭代记录

当前仓库保留了主要迭代提交：

- `4e02653`：用 `youtube-to-weixin-article` 替换原仓库内容。
- `58a5613`：补充 README 使用说明。
- `6aa075a`：写入新样本学习到的文章长度、标题、截图和字幕重组规则。
- `d1c8d4a`：修正来源展示规则，禁止在正文或长图页尾显示本地文件名。

本地技能同时维护在：

```text
N:\C\提纲图文版\skills\youtube-to-weixin-article
```

## 6. 当前技能内容

仓库中的技能由以下文件组成：

```text
README.md
PROJECT.md
EXPERIMENT.md
EVALUATION.md
SKILL_REVIEW.md
references/
evals/
assets/readme/hero.svg
youtube-to-weixin-article/
  SKILL.md
  agents/openai.yaml
  scripts/render_weixin_long_image.py
```

其中：

- `README.md`：GitHub 项目首页，面向新读者说明项目价值、安装、输入输出和实验依据。
- `PROJECT.md`：项目定位、目标用户、能力边界和使用方式。
- `EXPERIMENT.md`：实验过程和主要发现。
- `EVALUATION.md`：独立评分流程、评分标准和结果记录模板。
- `SKILL_REVIEW.md`：技能评审、问题定位、整改状态和发布检查。
- `references/`：已确认可复用的标题、结构、截图、长图和项目包装规则。
- `evals/`：独立评分、对照实验或评估摘要记录。
- `SKILL.md`：核心写作、截图、长图和来源规则。
- `agents/openai.yaml`：技能的展示信息。
- `render_weixin_long_image.py`：把 Markdown 文章渲染成公众号竖向长图 PNG 的辅助脚本。

长图默认参考样本规格：

- 1200px 宽
- 单栏文章布局
- 中文可读字号
- 正文不塞进窄卡片
- 截图按正文宽度嵌入
- 生成后需要视觉检查，避免文字出框、裁切、重叠或缺图

## 7. 验证方式

每次修改技能后，使用技能校验脚本检查格式：

```bash
python -X utf8 C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\Administrator\.codex\visualizations\2026\09\04\01a06a2b-21c1-7721-90dc-c1d26f1f82b3\podcast-article-full-flow\youtube-to-weixin-article
```

最近一次校验结果：通过。

长图相关规则还通过人工视觉检查迭代过，重点修正了：

- 文字不能超出容器
- 字号不能过小
- 长图不能缺少视频截图
- 页尾不能显示本地文件名

## 8. 当前结论

`youtube-to-weixin-article` 已经可以作为可下载技能使用。它把样本学习到的公众号文章结构、标题方法、截图选择、长图排版和来源展示规则固化在技能中。

后续文档维护标准：

- GitHub 首页 README 要先说明项目价值、证据链和使用路径。
- 实验文档要保留可复查的方法和结论，而不是只写“已经优化”。
- 外部 AI 或人工评分统一记录到 `EVALUATION.md`。
- 只有独立评分共性问题、明显可见问题或用户确认偏好，才写回技能。

后续如果继续增加样本，建议按同样方法追加观察：

- 新题材是否需要新的标题模板
- 小节数量是否需要根据 YouTube 时间轴进一步细化
- 哪类截图在公众号阅读中更有效
- 长图渲染脚本是否需要更接近真实公众号排版
